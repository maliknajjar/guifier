export class Field {
    public draw () {
        
    }

    public static getFieldInstance (name: string): Field {
        switch (name) {
            case value:
                
                break;
        
            default:
                break;
        }
    }
}
